import type { Alignment, WebsiteSection, WebsiteSectionProps } from "@/types/website";
type Props={selectedSection:WebsiteSection;onUpdateProp:(key:keyof WebsiteSectionProps,value:string)=>void};
const fields: {label:string;key:"title"|"subtitle"|"buttonText"|"imageUrl"}[]=[{label:"Title",key:"title"},{label:"Subtitle",key:"subtitle"},{label:"Primary action",key:"buttonText"},{label:"Image URL",key:"imageUrl"}];
export default function SectionProperties({selectedSection,onUpdateProp}:Props){return <div className="mt-5 rounded-3xl border p-4"><h3 className="font-semibold">{selectedSection.type}</h3><div className="mt-4 space-y-3">{fields.map(field=><label key={field.key} className="block text-sm">{field.label}<input className="mt-1 w-full border p-2" value={selectedSection.props[field.key]} onChange={e=>onUpdateProp(field.key,e.target.value)}/></label>)}<div className="flex gap-2">{(["left","center","right"]as Alignment[]).map(value=><button key={value} onClick={()=>onUpdateProp("alignment",value)}>{value}</button>)}</div></div></div>}
