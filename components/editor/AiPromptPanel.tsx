import type { FormEvent } from "react";

type AiPromptPanelProps = { suggestions: string[]; prompt: string; onPromptChange: (value: string) => void; autoMode: boolean; onToggleAutoMode: () => void; onSubmit: (event: FormEvent) => void; isProcessing: boolean };

export default function AiPromptPanel({ suggestions, prompt, onPromptChange, autoMode, onToggleAutoMode, onSubmit, isProcessing }: AiPromptPanelProps) {
  return <div className="border-t border-zinc-200 bg-white/90 p-4"><div className="mb-3 flex flex-wrap gap-2">{suggestions.map((pill) => <button type="button" key={pill} onClick={() => onPromptChange(pill)} className="suggestion-chip rounded-full border border-zinc-200 bg-zinc-50 px-3 py-1.5 text-sm text-zinc-700 transition hover:bg-zinc-100">{pill}</button>)}</div><form onSubmit={onSubmit} className="chat-composer rounded-[24px] border border-zinc-200 bg-zinc-50 p-3 shadow-sm"><textarea rows={3} className="w-full resize-none bg-transparent text-sm text-zinc-700 outline-none" placeholder="What would you like to build?" value={prompt} onChange={(event) => onPromptChange(event.target.value)} /><div className="mt-3 flex items-center justify-between gap-2"><div className="flex items-center gap-2"><button type="button" className="composer-icon rounded-full border border-zinc-200 bg-white p-2 text-zinc-600">+</button><button type="button" onClick={onToggleAutoMode} className={`auto-mode-toggle rounded-full px-3 py-1.5 text-sm ${autoMode ? "bg-zinc-900 text-white" : "bg-white text-zinc-700"}`}>{autoMode ? "Auto" : "Discuss"}</button></div><div className="flex items-center gap-2"><button type="button" className="composer-icon rounded-full border border-zinc-200 bg-white p-2 text-zinc-600">🎤</button><button type="submit" disabled={isProcessing} className="rounded-full bg-zinc-900 px-4 py-2 text-sm font-medium text-white disabled:opacity-60">Send</button></div></div></form></div>;
}
