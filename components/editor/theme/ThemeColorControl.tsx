"use client";import{useState}from"react";export default function ThemeColorControl({label,value,defaultValue,onChange}:{label:string;value:string;defaultValue:string;onChange:(value:string)=>void}){const[copied,setCopied]=useState(false);return <label className="theme-color"><span>{label}</span><input type="color" value={value} onChange={e=>onChange(e.target.value)}/><input aria-label={`${label} hex`} value={value} onChange={e=>/^#[0-9a-f]{0,6}$/i.test(e.target.value)&&onChange(e.target.value)}/><button type="button" onClick={async()=>{await navigator.clipboard?.writeText(value);setCopied(true);setTimeout(()=>setCopied(false),900)}}>{copied?"Done":"Copy"}</button><button type="button" onClick={()=>onChange(defaultValue)}>Reset</button></label>}
