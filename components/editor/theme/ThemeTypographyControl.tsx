import type{WebsiteTheme}from"@/types/website";const fonts=["Inter","Geist","Arial","Georgia","Times New Roman","Trebuchet MS","Courier New"];export default function ThemeTypographyControl({theme,onChange}:{theme:WebsiteTheme;onChange:(patch:Partial<WebsiteTheme>)=>void}){return <div className="theme-grid"><label>Heading font<select value={theme.headingFont} onChange={e=>onChange({headingFont:e.target.value})}>{fonts.map(f=><option key={f}>{f}</option>)}</select></label><label>Body font<select value={theme.bodyFont} onChange={e=>onChange({bodyFont:e.target.value})}>{fonts.map(f=><option key={f}>{f}</option>)}</select></label></div>}
