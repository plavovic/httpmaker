import type { DesignPreset } from "@/types/designPreset";import type { ApplyPresetMode } from "@/utils/applyDesignPreset";export default function ApplyPresetDialog({preset,onApply,onClose}:{preset:DesignPreset;onApply:(mode:ApplyPresetMode)=>void;onClose:()=>void}){return <div className="preset-dialog-backdrop" role="presentation" onClick={onClose}><div role="dialog" aria-modal="true" className="preset-dialog" onClick={e=>e.stopPropagation()}><p className="text-xs uppercase opacity-60">Apply design direction</p><h3 className="mt-1 text-lg">{preset.name}</h3><button onClick={()=>onApply("restyle")}>Restyle current website<span>Keep content and section order</span></button><button onClick={()=>onApply("replace-layout")}>Start from preset layout<span>Replace with a complete example website</span></button><button disabled>Create a copy<span>Prototype · coming later</span></button><button onClick={onClose} className="cancel">Cancel</button></div></div>}
