import DesignPresetPreview from "./DesignPresetPreview";import type { DesignPreset } from "@/types/designPreset";export default function DesignPresetCard({preset,selected,onApply}:{preset:DesignPreset;selected:boolean;onApply:()=>void}){return <article className={`preset-card ${selected?"selected":""}`}><DesignPresetPreview preset={preset}/><div className="p-3"><div className="flex items-center justify-between"><h3>{preset.name}</h3><div className="flex gap-1">{[preset.theme.primaryColor,preset.theme.secondaryColor,preset.theme.accentColor].map(c=><i key={c} className="h-3 w-3 rounded-full" style={{background:c}}/>)}</div></div><p className="mt-1 text-xs opacity-70">{preset.description}</p><p className="mt-2 text-[10px] opacity-60">{preset.theme.headingFont} · {preset.theme.bodyFont}</p><button type="button" onClick={onApply} className="mt-3 w-full border px-3 py-2 text-xs">Apply style</button></div></article>}
