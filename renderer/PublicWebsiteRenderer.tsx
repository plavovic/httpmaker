import SectionRenderer from "@/renderer/SectionRenderer";import type{WebsiteJSON}from"@/types/website";export default function PublicWebsiteRenderer({website}:{website:WebsiteJSON}){return <main className="min-h-screen" style={{backgroundColor:website.theme.backgroundColor,color:website.theme.textColor,fontFamily:website.theme.fontFamily}}><div className="mx-auto max-w-[1440px] space-y-4 px-4 py-4 sm:px-6 lg:px-8">{website.sections.map(section=><SectionRenderer key={section.id} section={section} theme={website.theme}/>)}</div></main>}
